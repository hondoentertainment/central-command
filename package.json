{
  "name": "central-command",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:firebase": "node scripts/build-firebase-config.js || true",
    "test": "node tests/run.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "dev": "npx serve . -p 3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
